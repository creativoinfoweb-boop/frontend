NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000

# Clerk (stessa istanza "Development" del dashboard — vedi API Keys)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...

# Opzionale: dove il proxy /api/webhook inoltra gli eventi Stripe (default = NEXT_PUBLIC_API_URL + /subscriptions/webhook/stripe)
# STRIPE_WEBHOOK_FORWARD_URL=http://127.0.0.1:8000/subscriptions/webhook/stripe
